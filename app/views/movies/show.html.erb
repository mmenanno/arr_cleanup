<!-- Back Navigation -->
<div class="space-y-8">
  <div>
    <%= link_to movies_path, class: "inline-flex items-center gap-2 text-indigo-400 hover:text-indigo-300 transition-colors cursor-pointer" do %>
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
      </svg>
      <span class="font-medium">Back to All Movies</span>
    <% end %>
  </div>

  <%= render "movies/show_details", movie: @movie %>
</div>
