<!DOCTYPE html>
<html class="h-full bg-gray-900">
  <head>
    <title><%= content_for(:title) || "Arr Cleanup" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@700;800&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.svg">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-900" data-controller="modal">
    <div class="min-h-full">
      <nav class="bg-gray-800 border-b border-gray-700">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="shrink-0">
                <%= link_to root_path, class: "flex items-center gap-3" do %>
                  <%= image_tag "logo.svg", alt: "Arr Cleanup Logo", class: "h-8 w-8" %>
                  <h1 class="text-2xl font-extrabold tracking-tight" style="font-family: 'Inter', system-ui, -apple-system, sans-serif; background: linear-gradient(135deg, #60A5FA 0%, #3B82F6 50%, #93C5FD 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">Arr Cleanup</h1>
                <% end %>
              </div>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Dashboard", root_path, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white cursor-pointer" %>
                <%= link_to "Movies", movies_path, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white cursor-pointer" %>
                <%= link_to "Shows", shows_path, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white cursor-pointer" %>
                <%= link_to "Exports", exports_path, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white cursor-pointer" %>
                <%= link_to "Settings", edit_settings_path, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white cursor-pointer" %>
              </div>
            </div>
          </div>
        </div>
      </nav>

      <main class="py-10">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <%= yield %>
        </div>
      </main>
    </div>

    <!-- Toast Container -->
    <div id="toasts" class="fixed top-4 right-4 z-50 pointer-events-none space-y-4" aria-live="polite" aria-atomic="true">
    </div>

    <!-- Modal Container -->
    <%= render "shared/confirm_modal" %>
  </body>
</html>
